{
  "name": "ghost-beta",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous beta testers powered by Astra through the Codex SDK",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test tests/*.test.js",
    "benchmark": "node scripts/benchmark.js",
    "check": "node --check src/server.js && node --check src/runner.js && node --check public/app.js"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.154.0",
    "playwright": "1.63.0"
  }
}
